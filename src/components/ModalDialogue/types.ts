import { NameItemMenu } from 'components/App/types';

export interface IUserLocalsProperties {
  // currentItemMenu: NameItemMenu | null;
  // onChangeItemMenu: (newItem: NameItemMenu | null) => void;
}