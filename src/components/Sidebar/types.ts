import { SideMenuItemNames } from 'components/App/types';

export interface ISidebarProperties {
  // currentItemMenu: SideMenuItemNames | null;
  // onChangeItemMenu: (newItem: SideMenuItemNames | null) => void;
}